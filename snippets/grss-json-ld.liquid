<!-- SEO BY GOOGLE RICH SNIPPET -->{% comment %} startBreadcrumbGrss {% endcomment %}
{% if template.name == "collection" %}
<script type="application/ld+json">
{
	"@context": "https://schema.org",
	"@type": "BreadcrumbList",
	"itemListElement": [{
		"@type": "ListItem",
		"position": 1,
		"name": "{{shop.name}}",
		"item": "{{ shop.url }}"
		},{
		"@type": "ListItem",
		"position": 2,
		"name": "{{collection.title | strip_html | escape}}",
		"item": "{{ shop.url }}{{collection.url}}"
	}]
}
</script>
{% endif %}
{% if template.name == "product" %}
<script type="application/ld+json">
{
	"@context": "https://schema.org",
	"@type": "BreadcrumbList",
	"itemListElement": [{
		"@type": "ListItem",
		"position": 1,
		"name": "{{shop.name}}",
		"item": "{{ shop.url }}"
		},{
		"@type": "ListItem",
		"position": 2,
		"name": "{{product.title | strip_html | escape}}",
		"item": "{{ shop.url }}{{product.url}}"
	}]
}
</script>
{% endif %}
{% comment %} endBreadcrumbGrss {% endcomment %}
